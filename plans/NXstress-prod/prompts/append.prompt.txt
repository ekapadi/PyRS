We are moving from a prototype NXstress-conformant I/O implementation for PyRS, to a production hookup of the same (i.e. with respect to the PyRS GUI and backend itself).  Details about this hookup are in the implementation specs in 'plans/NXstress-prod', and the corresponding 'plans/NXstress-prod/open-questions'.  In addition to the implementation described by these specs, based on answers to several of the questions, I am planning on extending the existing NXstress implementation to *also* allow multiple `HidraWorkspace` to be used as input (and output), and in addition, to allow an _append_ mode.  Note that in these cases, we assume that as the current combined-index implementation is spanned over the multiple `HidraWorkspace` that there are no overlaps -- i.e. each workspace includes only unique scan points (and / or other fields).  What I need you to do as a first step is to take a look at our existing set of implementation plans, and see where this extension to NXstress fits into the sequence.  After I respond to that, we'll figure out the details of the associated story, so please don't go too far with those details initially, beyond what you need to figure out where it fits in.

With respect to the part of subspec[s] 04, that discuss extending the `NXstress` implementation for multiple-workspace I/O, and for append mode.
I wanted to suggest that we consider doing the following: (1) have the 'discriminator' coordinates be the most slowly varying coordinates in
the combined index; (2) deliver the *sorted* combined-index *only* in single-step output mode -- that is: all of the 'HidraWorkspace' and all of the 'PeakCollection' are present at the output call. (3) For append mode (which is a future proofing extension), sort to the degree possible, but don't *enforce*
that the combined index be sorted.  (To avoid any possible confusion: we still *DO* enforce the non-overlapping values invariant!) Note that this sorting really had nothing to do with the `NXstress` schema, and was only considered to be a "nice to have"
attribute.  I don't want to drop sorting entirely, but it should be OK to relax it for "append" mode.
Please do an analysis and let me know if this helps to simplify what's going on in the 04 subspec[s]?
